var helpersHelpers = require('./src/helpers');
var filtersHelpers = require('./src/filters');

module.exports = {
  helpers: helpersHelpers, // the dust-test-helper helper-tester
  filters: filtersHelpers // the dust-test-helper filter-tester
};
